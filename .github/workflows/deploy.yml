name: Deploy Junglebook

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy
        run: |
          cd /home/vboxuser/junglebook
          git pull origin main
          docker compose down
          docker compose up -d
          sleep 15
          docker compose restart nginx
