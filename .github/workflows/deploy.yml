name: Deploy Junglebook

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy
        run: |
          cd ~/junglebook
          git pull origin main
          docker compose down
          docker compose up -d
          sleep 15
          docker compose restart nginx
